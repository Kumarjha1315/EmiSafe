# Database connection (MongoDB Atlas URL for production deployment)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/emisafe?retryWrites=true&w=majority

# JWT secret key for authentication
JWT_SECRET=your_jwt_secret_key_here

# Server port (default 3000)
PORT=3000
